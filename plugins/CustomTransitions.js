import { createSimpleTransition } from 'vuetify/lib/components/transitions/createTransition'

export let fadeXWidthTransition = {
    name: 'fade-x-width-transition',
    obj: createSimpleTransition('fade-x-width-transition')
}